# Data Dictionary & Column Pile-Sort

**Source:** `data/raw/cps_filers_clean.csv` — 64,696 rows x 59 columns (IPUMS CPS ASEC 2024, income year 2023), one row per person.

> Auto-generated by `notebooks/build_modeling_table.ipynb` (Phase 0). Do not edit by hand — edit `COLUMN_PILES` in the notebook and re-run.

This is the **leakage firewall** required by the spec (README §3.4, §9 Phase 0): every column is assigned to exactly one pile. When column timing is ambiguous, it is quarantined.

## Pile summary

| Pile | Count | Meaning |
|---|---:|---|
| `target` | 1 | The value we predict. |
| `feature` | 16 | Pre-tax; enters the model matrix X (directly or via a derived column). |
| `optional_feature` | 7 | Legitimate pre-tax axis, out of scope for the locked framing; documented, not in X. |
| `quarantine` | 9 | Target components or post-tax/credit outputs; never a feature (leakage). |
| `weight` | 2 | Survey weight; weighted validation only, never in X. |
| `sensitive` | 8 | Sensitive axis excluded from the equity framing (§10.4). |
| `drop` | 16 | Identifier, admin, redundant/household-level, or a structural key. |
| **total** | **59** | |

## `target` (1)

| Column | Label | Rationale |
|---|---|---|
| `eff_rate` | Effective federal tax rate (DERIVED = fedtaxac/adjginc*100) | Prediction target: 100 x fedtaxac / adjginc (percent), scoped to the tax RETURN. |

## `feature` (16)

| Column | Label | Rationale |
|---|---|---|
| `inctot` | Total personal income | Pre-tax total personal income; the filer's own contribution to unit_inctot. |
| `ftotval` | Total family income | Pre-tax total FAMILY income. Sole recoverable source of the absent spouse's income (see Phase 1 step 4); enters X only via unit_inctot / the shares. |
| `incwage` | Wage and salary income | Source of wage_share (income composition; core equity signal). |
| `incbus` | Non-farm business income | Source of business_share; can be negative (losses). |
| `incint` | Income from interest | Source of interest_share. |
| `incdivid` | Income from dividends | Source of dividend_share; preferentially taxed capital income. |
| `incretir` | Retirement income | Source of retirement_share; nulls imputed to 0. |
| `incss` | Social Security income | Source of socsec_share; partially/untaxed. |
| `incrent` | Income from rent | Source of rent_share; can be negative (losses). |
| `filestat` | Tax filer status | Tax filer status. Codes 1/2/3 are all JOINT (split by age, per codebook) -> collapsed to filing_status. CPS-tax-model assigned; flagged (§3.4). |
| `marst` | Marital status | Marital status; structural driver independent of imputed filestat. |
| `nchild` | Number of own children in household | Number of own children; drives dependent credits/deductions. |
| `nchlt5` | Number of own children under age 5 in hh | Young children (<5); CTC / childcare relevance. |
| `famsize` | Number of own family members in hh | Family size; household scale. |
| `age` | Age | Age; retirement / senior treatment. |
| `statefip` | State (FIPS code) | State (FIPS); geographic axis. Categorical (51 levels) -> one-hot at model time. |

## `optional_feature` (7)

| Column | Label | Rationale |
|---|---|---|
| `educ` | Educational attainment recode | Education; context axis, not in the core income/filing/geography framing. |
| `empstat` | Employment status | Employment status; optional context axis. |
| `wkswork1` | Weeks worked last year | Weeks worked last year; optional context axis. |
| `uhrsworkly` | Usual hours worked per week (last yr) | Usual hours/week; optional context axis. |
| `classwkr` | Class of worker  | Class of worker (self-employed vs wage); optional context axis. |
| `pension` | Pension plan at work | Pension coverage; optional context axis. |
| `firmsize` | Number of employees | Firm size; optional context axis. |

## `quarantine` (9)

| Column | Label | Rationale |
|---|---|---|
| `fedtaxac` | Federal income tax liability, after all credits | Numerator of the target (federal tax after credits). |
| `adjginc` | Adjusted gross income | Denominator of the target (AGI). Used ONLY to validate feature scope, never in X. |
| `spmfedtaxac` | SPM unit's federal tax | SPM-unit federal tax; same outcome, different grouping. Leakage. |
| `eitcred` | Earned income tax credit | Refundable credit; computed as part of the tax outcome. |
| `ctccrd` | Child Tax Credit | Child Tax Credit; post-tax credit. |
| `actccrd` | Additional Child Tax Credit | Additional (refundable) CTC; post-tax credit. |
| `margtax` | Federal income marginal tax rate | Marginal tax rate; direct function of the tax computation. |
| `taxinc` | Taxable income amount | Taxable income; post-deduction, downstream of AGI. |
| `fica` | Social security retirement payroll deduction | Payroll tax; a computed tax outcome (kept out to keep 'pre-tax' clean). |

## `weight` (2)

| Column | Label | Rationale |
|---|---|---|
| `asecwt` | Annual Social and Economic Supplement Weight | ASEC person weight; population-representative validation. Never in X. |
| `asecwth` | Annual Social and Economic Supplement Household weight | ASEC household weight; validation weight. Never in X. |

## `sensitive` (8)

| Column | Label | Rationale |
|---|---|---|
| `sex` | Sex | Sensitive; include only if the equity framing explicitly covers it (it does not). |
| `race` | Race | Race; not an agreed equity axis. Never a causal driver in this project. |
| `hispan` | Hispanic origin | Hispanic origin; not an agreed equity axis. |
| `citizen` | Citizenship status | Citizenship; immigration axis, out of framing. |
| `nativity` | Foreign birthplace or parentage | Nativity; immigration axis, out of framing. |
| `yrimmig` | Year of immigration | Year of immigration; immigration axis, out of framing. |
| `vetstat` | Veteran status | Veteran status; sensitive, out of framing. |
| `diffany` | Any difficulty | Disability; sensitive, out of framing. |

## `drop` (16)

| Column | Label | Rationale |
|---|---|---|
| `year` | Survey year | Constant survey year; identifier/admin. |
| `serial` | Household serial number | Household serial number; identifier. Structural key for tax-unit assembly, not a feature. |
| `month` | Month | Survey month; admin. |
| `cpsid` | CPSID, household record | CPSID household record; identifier. |
| `cpsidp` | CPSID, person record | CPSID person record; identifier. |
| `cpsidv` | Validated Longitudinal Identifier | Validated longitudinal identifier; identifier. |
| `asecflag` | Flag for ASEC | ASEC flag; admin. |
| `pernum` | Person number in sample unit | Person number; identifier. Target of the depstat pointer, not a feature. |
| `ftype` | Family Type | Family type; administrative grouping. |
| `spmfamunit` | SPM unit's ID | SPM unit id; grouping identifier. NOT used to key the family (an SPM unit can span several families -- 4,173 do). |
| `spmwt` | SPM unit's weight | SPM unit weight; not used (filer-level analysis). |
| `spmftotval` | SPM unit's cash income | SPM unit cash income; wrong grouping for the tax unit (ftotval is used instead). |
| `hhincome` | Total household income | Total household income; household spans multiple tax units, too coarse. |
| `occ2010` | Occupation, 2010 basis | Occupation (479 levels); not in the §3.4 feature set. |
| `ind` | Industry | Industry; not in the §3.4 feature set. |
| `depstat` | Dependency status pointer | Dependency-status pointer: selects filer units (==0) and assigns dependents to their filer. Structural, not a feature; constant (0) after filtering. |

## Unit of analysis: the tax RETURN, not the person

The target is scoped to the whole filing unit — `fedtaxac` and `adjginc` are imputed by Census at the **filing** level — while every income column on a filer row is that one **person's** own. Before reconstruction, the median `adjginc / inctot` was **1.77** for joint filers against **1.00** for single filers: the model would have been asked to predict a rate whose denominator it could not see, for ~40% of rows.

Income is therefore rebuilt at return scope before any feature is derived from it:

| Piece | How it is recovered |
|---|---|
| Filer | Their own row. |
| Dependents | Present as rows; `depstat` points at the claiming filer's `pernum` within `serial`. |
| Spouse | **Absent from the extract entirely** — one spouse is kept per couple. Recovered as the family residual `ftotval - sum(inctot of persons present)`. |

The residual is the spouse, not noise: families with **no** joint filer show a residual of $0 at the 25th, 50th and 75th percentile; families with one show a median of **~$50,000**. It is keyed on the **family** (`ftotval` is a family total), not on `spmfamunit` — an SPM unit can span several families, and 4,173 do. Keying on `spmfamunit` yields 2,978 impossible negative residuals versus 148 on the family key (clipped to 0).

| Scope check (median ratio, 1.00 = aligned) | joint | head | single | spread |
|---|---:|---:|---:|---:|
| before — `adjginc / inctot` | 1.77 | 1.00 | 1.00 | 0.77 |
| after — `adjginc / unit_inctot` | 1.00 | 1.00 | 1.00 | 0.12 |

Correlation with `adjginc`: **0.815 -> 0.982**. `adjginc` is quarantined and is used here purely as a ruler to check scope — it never enters `X`. A better-scoped *pre-tax income* feature is not leakage: income level is the primary legitimate driver of an effective rate, and knowing the denominator of a ratio does not reveal the ratio.

## Modeling feature set `X` (built in Phase 1)

Only `feature`-pile columns are used, and every engineered column is checked back to the pile of each raw column it is built from. Raw income amounts are transformed into **income-composition shares** (the core equity signal); the raw amounts do not enter `X`.

Each share is `(filer + dependents for that source) / unit_inctot`, set to **0 when `unit_inctot < 1000`**: below that floor the denominator is too small and shares explode (e.g. `inctot=$50, incint=$500` -> 1000%), producing nonsense splits and nonsense SHAP attributions. Shares stay negative for genuine business/rent losses above the floor (kept, §3.4).

The spouse's income is recoverable only as a **total** — `ftotval` carries no breakdown by source — so it cannot be distributed across the seven shares. It gets its own column, `spouse_income_share`, which keeps the shares additive and makes the unobserved portion legible to SHAP as its own bar instead of silently deflating the other seven for every joint filer.

| Feature | Type | Derivation |
|---|---|---|
| `unit_inctot` | numeric | `inctot + dependents' inctot + spouse residual` — pre-tax income of the whole return. |
| `wage_share` | numeric | `(unit incwage) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `business_share` | numeric | `(unit incbus) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `interest_share` | numeric | `(unit incint) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `dividend_share` | numeric | `(unit incdivid) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `retirement_share` | numeric | `(unit incretir) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `socsec_share` | numeric | `(unit incss) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `rent_share` | numeric | `(unit incrent) / unit_inctot`, 0 when `unit_inctot < 1000`. |
| `spouse_income_share` | numeric | `spouse residual / unit_inctot` — the portion of unit income with no source breakdown; 0 for non-joint filers. |
| `filing_status` | categorical | `filestat` collapsed: **1=joint (raw 1/2/3), 4=head_of_household, 5=single**. |
| `marst` | categorical | Marital status. |
| `statefip` | categorical | State FIPS (51 levels) -> one-hot at model time. |
| `nchild` | numeric | Number of own children. |
| `nchlt5` | numeric | Own children under 5. |
| `famsize` | numeric | Family size. |
| `age` | numeric | Age. |

**Target:** `eff_rate` (percent). **Retained non-feature column:** `asecwt` (weighted validation, §3.5).

### Why `filestat` was collapsed

README §3.4 glosses the codes as `1=joint, 2=joint/sep, 3=sep`. The IPUMS codebook shipped at `data/raw/data_dictionary.csv` says otherwise:

| Code | Codebook | In this file |
|---|---|---|
| 1 | Joint, both < 65 | 100% `marst`=1, ages 17–64 |
| 2 | Joint, one < 65, one 65+ | 100% `marst`=1, median age 65 |
| 3 | Joint, both 65+ | 100% `marst`=1, **minimum age 65** |
| 4 | Head of household | mixed `marst` |
| 5 | Single | mixed `marst` |

Codes 1/2/3 are the **same filing status split by age**, and there is no married-filing-separately category in this data at all. Keeping the raw code would hand the model an age bracket disguised as a filing status — redundant with `age`, which is already a feature — and would make the twin comparison incoherent: flipping `5 -> 3` means *become married **and** become 65+*, a gap no single attribute could own.

## Filer-unit filter, target & negative rates

- **Filer unit (§3.2, option A + reconstruction):** keep `depstat == 0` (non-dependent filers) -> 61,231 rows, each rebuilt to whole-return income scope.
- **Target (§3.3):** `eff_rate = 100 x fedtaxac / adjginc`; all rows have `adjginc > 0`, so it is always defined.
- **Negative rates (§3.3):** ~12% of filer rows have `fedtaxac < 0` (refundable credits exceed tax owed). These negative effective rates are **kept, not clipped** — the net-progressive tail is a finding.

